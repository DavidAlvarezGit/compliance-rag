name: Ping Streamlit

on:
  schedule:
    - cron: "*/10 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Curl request
        run: |
          curl https://your-app.streamlit.app/
